


编译

JVM 将java文件编译为.class文件，生成JVM可执行的字节码。
编译时，会将类的常量放入常量池。
常量池是存储在方法区中的。
编译一个类时，如果这个类所依赖的类还没有被编译，编译器就会先编译这个被依赖的类，然后引用，否则直接引用。


加载
包含：加载、验证、准备、解析、初始化、使用、卸载 七个过程

类变量有两次赋初始值得过程
一个是在准备阶段，赋予系统默认值
另一个是在初始化阶段，赋予程序员定义的初始值。
因此，即使在初始化阶段程序员没有赋值也没关系，类变量仍然有一个默认初始值。